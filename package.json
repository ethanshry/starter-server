{
  "name": "starter-server",
  "version": "0.0.0",
  "description": "basic server to get you started with typescript + express",
  "main": "server.js",
  "scripts": {
    "start": "nodemon --exec npm run build-dev",
    "build": "tsc",
    "build-dev": "npm run build && node ./dist/server.js"
  },
  "author": "ethanshry",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3"
  },
  "devDependencies": {
    "@types/express": "^4.16.0",
    "@types/node": "^8.9.4",
    "nodemon": "^1.17.2",
    "tslint": "^5.9.1",
    "tslint-eslint-rules": "^5.1.0",
    "typescript": "^2.9.1"
  }
}
